let name = "keshav" //string
let age = 19  //number
let isLoggedIn = false //boolean
let date //undefined(its a type)
let temp = null // null type is object *note:null(standalone value) //example:if user ask a temprature if we dont have the value we can send null insted of o(its also a degree so we cant use 0 (according to this senario))
let numb = 12345678912345588n//bigint //another way is let numb = BigInt(12345678912345588)  *note: js only handel 15 digits of number so bigint is introduced in 2020



console.log(typeof name)
console.log(typeof age)
console.log(typeof numb)
console.log(typeof isLoggedIn)
console.log(typeof date)
console.log(typeof temp)